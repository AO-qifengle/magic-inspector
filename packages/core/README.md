# Magic Inspector Core

Shared TypeScript primitives for WebRTC leak classification and network-quality scoring.

The package contains no network clients and never sends data. Install from GitHub Packages:

```sh
npm install @ao-qifengle/magic-inspector-core --registry=https://npm.pkg.github.com
```
